[tool.ruff]
line-length = 100

[tool.ruff.lint]
ignore = [
    # Selenium 크롤링은 stale element, timeout 등 다양한 예외를
    # 폭넓게 잡아 재시도해야 하므로 broad except를 의도적으로 사용한다.
    "BLE001",

    # 파일명에 쓰는 날짜는 실행 서버의 로컬 날짜 기준이면 충분하고
    # 타임존 인식 datetime이 필요하지 않다.
    "DTZ005",
]
